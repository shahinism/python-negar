language: python
python:
  - "2.6"
  - "2.7"
before_install:
 - pip install pyside
# command to install dependencies
install: "python setup.py install"
# # command to run tests
script: negar -f tests/input_file -o tests/output_file

